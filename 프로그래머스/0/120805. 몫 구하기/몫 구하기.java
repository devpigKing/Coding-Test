class Solution {
    public int solution(int num1, int num2) { // solution이라는 함수에 int형 변수 num1, num2를 입력받음.
        int answer = 0; // 결괏값을 담아둘 answer 변수를 선언 후 초기화 
        answer = num1 / num2; // num1 / num2의 값을 answer에 할당
        return answer; // solution 함수의 결괏값으로 answer 반환
    }
}